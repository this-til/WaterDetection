<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.til.water_detection.sql.mapper.ICommandMapper">
    <select id="getCommandByIdArray" resultType="com.til.water_detection.data.Command">
        select *
        from water_detection_data.command
        where id IN
        <foreach collection="id" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>
</mapper>